const infoBar = () => {
  return (
    // a 200 pixel wide and full height div that is fixed to the right side of the screen
    <div className="fixed right-0 w-64 h-full bg-gray-400">
      {/* Your content goes here */}
      Test
    </div>
  );
};

export default infoBar;
